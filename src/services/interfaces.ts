export interface Podcast {
  id: string;
  title: string;
  autor: string;
  imageSource: string;
  description: string;
}

export interface Episode {
  title: string;
  date: string;
  duration: string;
  id: string;
}
